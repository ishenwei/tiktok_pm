# docker-compose.yml - 修正版本
version: '3.8'

services:

  # 1. Django 应用服务 (Web)
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tiktok_pm_web

    # 🌟 恢复代码映射，以便执行 gunicorn 🌟
    volumes:
      - .:/app  # 假设 WORKDIR 为 /app
      - static_volume:/app/staticfiles
      - media_volume:/app/mediafiles
      # 🌟 挂载 Python 补丁 🌟
      - ./patches/django_q/core_signing.py:/usr/local/lib/python3.12/site-packages/django_q/core_signing.py

    command: gunicorn tiktok_pm_project.wsgi:application --bind 0.0.0.0:8000 --workers 3 --timeout 120

    expose:
      - 8000
    env_file:
      - .env

    # 确保数据库启动
    depends_on:
      - db # 假设 db 服务存在

  # 2. Nginx 反向代理和静态文件服务
  nginx:
    image: nginx:latest
    container_name: tiktok_pm_nginx
    ports:
      - "8888:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - static_volume:/app/staticfiles
      - media_volume:/app/mediafiles
    depends_on:
      - web

  # 3. 🌟 Django Q Worker 服务 (运行 qcluster) 🌟
  worker:
    build: .
    container_name: tiktok_pm_worker

    # 🌟 恢复代码映射，以便执行 manage.py qcluster 🌟
    volumes:
      - .:/app # 假设 WORKDIR 为 /app
      # 🌟 挂载 Python 补丁 🌟
      - ./patches/django_q/core_signing.py:/usr/local/lib/python3.12/site-packages/django_q/core_signing.py

    # 🌟 添加环境变量 🌟
    env_file:
      - .env

    command: python manage.py qcluster

    depends_on:
      - web

# Docker 卷定义
volumes:
  static_volume:
  media_volume: